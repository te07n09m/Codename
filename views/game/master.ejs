<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/game.css">
    <title>マスター | コードネーム</title>
</head>
<body>
    <main>
        <div class="content-box">
            <p>MASTER:<%= game._id %></p>
            <div class="grid-box">
                <% for(let i = 0; i < 25; i++) { %>
                    <div class="card <%= game.colors[i] %>">
                        <p class="word"><%= game.words[i] %></p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <script src="/js/resizeWord.js"></script>
</body>
</html>
